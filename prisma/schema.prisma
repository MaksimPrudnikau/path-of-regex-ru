generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model maps {
  id          Int  @id
  name        String
  rank        Int
  regex       String
  uniqueIndex Int
}
